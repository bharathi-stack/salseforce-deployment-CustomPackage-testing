# name: Salesforce CI/CD Pipeline
# on:
#   push:
#     branches:
#       - dev

# jobs:
#   # ========================================
#   # 1. FIRST JOB: Initial Process (Validate Code)
#   # ========================================
#   validate_code:
#     name: 1. Validate Code (Prettier & PMD)
#     runs-on: ubuntu-latest
#     steps:
#       - name: Check out code
#         uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "20"

#       - name: Install npm dependencies
#         run: npm install

#       - name: Install Salesforce CLI
#         uses: sfdx-actions/setup-sfdx@v1

#       # --- ADDED FIX ---
#       # Force-update the CLI to repair any missing internal files
#       - name: Force-update Salesforce CLI
#         run: sf update
#       # --- END OF FIX ---

#       - name: Run Prettier code format check
#         run: npm run prettier:check
#       - name: Run PMD code scan
#         run: sfdx scanner:run --target "force-app,force-app2" --engine "pmd" --violations-cause-build-failure

#   # ========================================
#   # 2. SECOND JOB: Sandbox Validation
#   # ========================================
#   validate_sandbox:
#     name: 2. Validate against Sandbox
#     runs-on: ubuntu-latest
#     needs: validate_code

#     environment:
#       name: sandbox
#       url: ${{ steps.auth_sandbox.outputs.org_url }}

#     steps:
#       - name: Check out code
#         uses: actions/checkout@v4

#       - name: Install Salesforce CLI
#         uses: sfdx-actions/setup-sfdx@v1

#       # --- ADDED FIX ---
#       # Force-update the CLI to repair any missing internal files
#       - name: Force-update Salesforce CLI
#         run: sf update
#       # --- END OF FIX ---

#       - name: Authenticate to Sandbox
#         id: auth_sandbox
#         run: |
#           echo "${{ secrets.SFDX_SANDBOX_AUTH_URL }}" > sfdx_auth_file.txt
#           sfdx auth:sfdxurl:store -f sfdx_auth_file.txt -a SandboxOrg -s
#           org_url=$(sfdx force:org:display -u SandboxOrg --json | jq -r .result.sfdxAuthUrl)
#           echo "org_url=$org_url" >> $GITHUB_OUTPUT

#       - name: Validate against Sandbox (RunLocalTests)
#         run: |
#           sf project deploy validate \
#             --source-dir force-app \
#             --source-dir force-app2 \
#             --target-org SandboxOrg \
#             --test-level RunLocalTests \
#             --wait 60 \
#             --verbose

#   # ========================================
#   # 3. THIRD JOB: Production Deployment
#   # ========================================
#   deploy_production:
#     name: 3. Deploy to Production
#     runs-on: ubuntu-latest
#     needs: validate_sandbox

#     environment:
#       name: production
#       url: ${{ steps.auth_prod.outputs.org_url }}

#     steps:
#       - name: Check out code
#         uses: actions/checkout@v4

#       - name: Install Salesforce CLI
#         uses: sfdx-actions/setup-sfdx@v1

#       # --- ADDED FIX ---
#       # Force-update the CLI to repair any missing internal files
#       - name: Force-update Salesforce CLI
#         run: sf update
#       # --- END OF FIX ---

#       - name: Authenticate to Production
#         id: auth_prod
#         run: |
#           echo "${{ secrets.SFDX_PROD_AUTH_URL }}" > sfdx_auth_file.txt
#           sfdx auth:sfdxurl:store -f sfdx_auth_file.txt -a ProdOrg -s
#           org_url=$(sfdx force:org:display -u ProdOrg --json | jq -r .result.sfdxAuthUrl)
#           echo "org_url=$org_url" >> $GITHUB_OUTPUT

#       - name: Deploy to Production (RunLocalTests)
#         run: |
#           sf project deploy start \
#             --source-dir force-app \
#             --source-dir force-app2 \
#             --target-org ProdOrg \
#             --test-level RunLocalTests \
#             --wait 60 \
#             --verbose




name: Salesforce CI/CD Pipeline
on:
  push:
    branches:
      - dev

jobs:
  # ========================================
  # 1. FIRST JOB: Initial Process (Validate Code)
  # ========================================
  validate_code:
    name: 1. Validate Code (Prettier & PMD)
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install npm dependencies
        run: npm install

      - name: Install Salesforce CLI
        uses: sfdx-actions/setup-sfdx@v1

      - name: Force-update Salesforce CLI
        run: sf update

      - name: Run Prettier code format check
        run: npm run prettier:check
      - name: Run PMD code scan
        run: sfdx scanner:run --target "force-app,force-app2" --engine "pmd" --violations-cause-build-failure

  # ========================================
  # 2. SECOND JOB: Generate Delta & Validate
  # ========================================
  validate_sandbox:
    name: 2. Generate Delta & Validate
    runs-on: ubuntu-latest
    needs: validate_code

    environment:
      name: sandbox
      url: ${{ steps.auth_sandbox.outputs.org_url }}
    
    outputs:
      delta_exists: ${{ steps.find_files.outputs.delta_exists }}

    steps:
      - name: Check out code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Required for git diff to see previous commits

      - name: Install Salesforce CLI
        uses: sfdx-actions/setup-sfdx@v1

      - name: Force-update Salesforce CLI
        run: sf update
      
      # <-- MODIFICATION: Removed sfdx-git-delta install step

      - name: Authenticate to Sandbox
        id: auth_sandbox
        run: |
          echo "${{ secrets.SFDX_SANDBOX_AUTH_URL }}" > sfdx_auth_file.txt
          sfdx auth:sfdxurl:store -f sfdx_auth_file.txt -a SandboxOrg -s
          org_url=$(sfdx force:org:display -u SandboxOrg --json | jq -r .result.sfdxAuthUrl)
          echo "org_url=$org_url" >> $GITHUB_OUTPUT

      - name: Find changed metadata files # <-- MODIFICATION
        id: find_files
        run: |
          echo "Finding changes between HEAD~1 and HEAD"
          git diff --name-only HEAD~1 HEAD > changed-files.txt
          
          echo "--- All changed files ---"
          cat changed-files.txt
          echo "-------------------------"

          # Filter for files in force-app OR force-app2 OR the project file
          grep -E "(force-app/|force-app2/|sfdx-project.json)" changed-files.txt > salesforce-changes.txt || true
          
          echo "--- Salesforce changes ---"
          cat salesforce-changes.txt
          echo "--------------------------"

          if [ -s salesforce-changes.txt ]; then
            echo "Salesforce changes detected."
            echo "delta_exists=true" >> $GITHUB_OUTPUT
          else
            echo "No Salesforce metadata changes detected. Skipping validation."
            echo "delta_exists=false" >> $GITHUB_OUTPUT
          fi
      
      # <-- MODIFICATION: Removed artifact upload step

      - name: Validate Delta against Sandbox # <-- MODIFICATION
        if: steps.find_files.outputs.delta_exists == 'true'
        run: |
          echo "Validating delta changes..."
          
          # Build the --source-path arguments from the file list
          # xargs reads each line from the file and prepends '-p' (short for --source-path)
          # The output is a single line: -p path/one -p path/two
          SOURCE_PATHS=$(xargs -a salesforce-changes.txt -I {} echo -p {})
          
          if [ -z "$SOURCE_PATHS" ]; then
            echo "No paths to validate."
            exit 0
          fi
          
          echo "Validating paths: $SOURCE_PATHS"
          
          sf project deploy validate \
            $SOURCE_PATHS \
            --target-org SandboxOrg \
            --test-level RunLocalTests \
            --wait 60 \
            --verbose

  # ========================================
  # 3. THIRD JOB: Production Deployment
  # ========================================
  deploy_production:
    name: 3. Deploy to Production
    runs-on: ubuntu-latest
    needs: validate_sandbox
    
    if: needs.validate_sandbox.outputs.delta_exists == 'true'

    environment:
      name: production
      url: ${{ steps.auth_prod.outputs.org_url }}

    steps:
      - name: Check out code # <-- MODIFICATION: Need to checkout again to run git diff
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 

      - name: Install Salesforce CLI
        uses: sfdx-actions/setup-sfdx@v1

      - name: Force-update Salesforce CLI
        run: sf update

      - name: Authenticate to Production
        id: auth_prod
        run: |
          echo "${{ secrets.SFDX_PROD_AUTH_URL }}" > sfdx_auth_file.txt
          sfdx auth:sfdxurl:store -f sfdx_auth_file.txt -a ProdOrg -s
          org_url=$(sfdx force:org:display -u ProdOrg --json | jq -r .result.sfdxAuthUrl)
          echo "org_url=$org_url" >> $GITHUB_OUTPUT

      # <-- MODIFICATION: Removed artifact download
      
      - name: Find changed metadata files for Prod # <-- MODIFICATION
        id: find_files_prod
        run: |
          echo "Finding changes between HEAD~1 and HEAD for Prod deployment"
          git diff --name-only HEAD~1 HEAD > changed-files-prod.txt
          grep -E "(force-app/|force-app2/|sfdx-project.json)" changed-files-prod.txt > salesforce-changes-prod.txt || true
          echo "--- Salesforce changes to deploy ---"
          cat salesforce-changes-prod.txt
          echo "------------------------------------"

      - name: Deploy Delta to Production # <-- MODIFICATION
        run: |
          echo "Deploying delta changes to Production..."
          
          SOURCE_PATHS=$(xargs -a salesforce-changes-prod.txt -I {} echo -p {})
          
          if [ -z "$SOURCE_PATHS" ]; then
            echo "No paths to deploy."
            exit 0
          fi

          echo "Deploying paths: $SOURCE_PATHS"
          
          sf project deploy start \
            $SOURCE_PATHS \
            --target-org ProdOrg \
            --test-level RunLocalTests \
            --wait 60 \
            --verbose